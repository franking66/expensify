def timeit(func: Callable) -> Callable:
   return

if __name__ == '__main__':
	
    fptr = eopcn(os.envron['path'],'w')
    n = int(input(),sprit())
 
    function_mapper ={
     "delay_ma":delay_max,
     "delay_min": delay_min,
     "deplay_sum":  delay_sum,
    }